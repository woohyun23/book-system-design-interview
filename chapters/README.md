# Chapters

책의 각 챕터를 순서대로 정리합니다.

## 폴더 규칙

- 폴더 이름은 `chapter-01`, `chapter-02` 형식을 사용합니다.
- 챕터 제목과 번호는 각 폴더의 `README.md`에 작성합니다.
- 챕터별 실습은 해당 폴더의 `practice/`에 저장합니다.
- 챕터에서만 사용하는 이미지는 해당 폴더의 `images/`에 저장합니다.

새 챕터를 시작할 때 `templates/chapter-template.md`를 복사해 사용합니다.
